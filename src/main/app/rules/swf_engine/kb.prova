cardNumber("ZHILI","0267005").

validity("ZHILI","valide").


hasAcctID('never86','20100810').

isLessThan(Arg1,Arg2) :-
   java_less(Arg1,Arg2,Result),
   isTrue(Result).
   
isMoreThan(Arg1,Arg2) :-
   java_less(Arg1,Arg2,Result),
   isFalse(Result).
   
isTrue(true).

isFalse(false).
   
java_less(A, B, R) :- 	R = rbsla.utils.Math.less(A, B), !.
